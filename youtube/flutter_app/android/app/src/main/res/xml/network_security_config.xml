<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LAN HTTP (nodos Lanflix); en producción se puede restringir a rangos privados. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
